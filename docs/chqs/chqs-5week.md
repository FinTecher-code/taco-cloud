# 第五周 - 刷题记录

---

## 2026-08-10

---

### Q1 — OpenSSH 客户端和服务器的全局配置文件

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** OpenSSH客户端和服务器的两个全局配置文件是什么？

**选项:**
1. `/etc/ssh/ssh_config` 和 `/etc/ssh/sshd_config` ✅
2. `~/.sshconfig` 和 `/etc/ssh_config`
3. `~/.ssh/config` 和 `/etc/ssh.config`
4. `~/.ssh` 和 `/users/ssh/ssh_config`

**我的答案:** 选项3 ❌

**正确答案:** 选项1

**解析:**
- OpenSSH 客户端 的全局配置文件是 /etc/ssh/ssh_config。
- OpenSSH 服务器 的配置文件是 /etc/ssh/sshd_config。

---
### Q2 — dig 命令指定 DNS 服务器

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** 下列哪个命令将使用example1.com上的 ns1 DNS服务器，返回example2.com域上的域名系统（DNS）数据？

**选项:**
1. `dig @ns1.example1.com example2.com` ✅
2. `dig =ns1.example1.com example2.com`
3. `dig ns1.example2.com`
4. `dig ns1.example1.com example2.com`

**我的答案:** 选项1 ✅

**正确答案:** 选项1

**解析:**
- dig 是 Linux/Unix 中查询 DNS 记录的工具，指定 DNS 服务器的语法固定：`dig @<DNS服务器地址> <目标域名>`

---
### Q3 — shebang 行的作用

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** Linux脚本中shebang行的目的是什么(例如：`#!/bin/bash`)？

**选项:**
1. 识别想要使用的shell解释器 ✅
2. 识别脚本的最后一行
3. 启动shell解释器
4. 识别脚本的第一行

**我的答案:** 选项1 ✅

**正确答案:** 选项1

**解析:**
- Shebang 行（例如 #!/bin/bash）位于脚本的第一行，其目的是告诉系统应该使用哪个解释器来执行这个脚本文件。

---
### Q4 — killall 按名称停止进程

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** 下列哪个Linux命令，会根据名称停止正在运行的进程？

**选项:**
1. `purge`
2. `killall` ✅
3. `exit`
4. `rest`

**我的答案:** 选项2 ✅

**正确答案:** 选项2

**解析:**
- killall 命令可以根据进程名称向所有匹配的进程发送信号（默认 SIGTERM），从而停止进程。

---
### Q5 — /etc/resolv.conf 的用途

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** Linux 中，/etc/resolv.conf 的用途是？

**选项:**
1. 邮件服务的设置文件
2. DHCP的设置文件
3. DNS解析的设置文件 ✅
4. 网络路由的设置文件

**我的答案:** 选项4 ❌

**正确答案:** 选项3

**解析:**
- /etc/resolv.conf 文件是用于配置域名系统（DNS）解析相关设置的文件。它主要包含了 DNS 服务器的信息，告诉系统在进行域名解析时应该向哪些 DNS 服务器发送请求。

---
### Q6 — 写时拷贝（Copy-on-Write）的错误描述

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** 以下关于写时拷贝的描述，哪一项是错误的？

**选项:**
1. 写时拷贝是一种可以推迟甚至免除拷贝数据的技术
2. 创建子进程时会复制整个地址空间 ✅
3. 资源的复制只有在需要写入的时候才进行，在此之前，只是以只读方式共享
4. 这种技术使地址空间的页的拷贝被推迟到实际发生写入的时候

**我的答案:** 选项3 ❌

**正确答案:** 选项2

**解析:**
- 创建子进程时并不复制整个地址空间，而是让父进程和子进程共享一个拷贝

---
### Q7 — Linux 中 clone 和 fork 的区别

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** Linux 中，clone 和 fork 的区别描述正确的有？

**选项:**
1. clone和fork最大不同在于fork不再复制父进程的栈空间，而是自己创建一个新的
2. clone和fork最大不同在于clone不再复制父进程的栈空间，而是自己创建一个新的
3. clone是fork的升级版本，不仅可以创建进程或者线程，还可以指定创建新的命名空间（namespace）、有选择的继承父进程的内存、甚至可以将创建出来的进程变成父进程的兄弟进程等等 ✅
4. fork是clone的升级版本，不仅可以创建进程或者线程，还可以指定创建新的命名空间（namespace）、有选择的继承父进程的内存、甚至可以将创建出来的进程变成父进程的兄弟进程等等

**我的答案:** 选项3 ✅

**正确答案:** 选项3

**解析:**
- `clone`是一个更灵活的系统调用。它可以用于创建进程或线程，并且可以通过参数指定创建新的命名空间（`namespace`），可以有选择地继承父进程的内存，还可以通过设置参数使创建出来的进程成为父进程的兄弟进程等多种复杂的进程创建场景。

---
### Q8 — find 命令查找文件

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** 查找服务器上是否存在test.txt文件的命令是？

**选项:**
1. grep / -name test.txt
2. find / -name test.txt ✅
3. find / -file test.txt
4. find -name test.txt /

**我的答案:** 选项2 ✅

**正确答案:** 选项2

**解析:**
- 查找服务器上是否存在test.txt文件的命令是:find / -name test.txt

---
### Q9 — 孤儿进程的错误描述

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** 以下关于 Linux 中孤儿进程的描述，哪一项是错误的？

**选项:**
1. 父进程退出，其子进程还在运行，子进程成为孤儿进程
2. 孤儿进程被init进程收养
3. 孤儿进程的资源由init进程回收
4. 父进程没有调用wait或waitpid获取子进程信息，导致结束的子进程的描述符存在于系统中，子进程被称为孤儿进程 ✅

**我的答案:** 选项3 ❌

**正确答案:** 选项4

**解析:**
- 子进程退出了，但是父进程没有用wait或waitpid去获取子进程的状态信息，那么子进程的进程描述符仍然保存在系统中，这种进程称为僵尸进程。

---
### Q10 — 线程的错误说法

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** 下列关于 Linux 中线程的相关说法，错误的是？

**选项:**
1. 一个线程崩溃，不会导致其所属进程的其他线程崩溃 ✅
2. 在进行IO密集型处理时，多个线程允许计算和IO处理彼此重叠进行，会加快整体程序的执行速度
3. 进程间的通信也很复杂，并且维护进程的系统开销较大
4. 引入多线程编程，可以并发执行也可以共享相同的地址空间

**我的答案:** 选项2 ❌

**正确答案:** 选项1

**解析:**
- 由于线程要依附于进程存在，一旦一个线程崩溃，会导致其所属进程的所有线程崩溃。

---
### Q11 — GBase 8s 客户端命令行工具

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** GBase 8s用于连接数据库,并可执行sql的客户端命令行工具是?

**选项:**
1. onstat
2. onmode
3. oninit
4. dbaccess ✅

**我的答案:** 选项1 ❌

**正确答案:** 选项4

**解析:**
- GBase 8s用于连接数据库，并可执行sql的客户端命令行工具是dbaccess

---
### Q12 — GBase 8a MPP Cluster 加载功能特性

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** GBase 8a MPP Cluster 中，集群加载功能直接集成在 GBase 8a MPP Cluster 内部，不需要额外部署外部加载工具。数据加载不具备哪项特性？

**选项:**
1. 与集群高度集成
2. 支持单表多数据源并行加载，不支持多加载机对单表的并行加载，最大化提升加载性能 ✅
3. 支持普通文本、定长文本、宽松模式的加载
4. 支持错误数据溯源功能，可以准确定位错误数据在源文件中的位置

**我的答案:** 选项4 ❌

**正确答案:** 选项2

**解析:**
- GBase 8a MPP Cluster 数据加载具备如下一些特性和优点：
- 1）与集群高度集成；
- 2）面向用户的 SQL 接口方式更符合用户的使用习惯；
- 3）支持单表多数据源并行加载，支持多加载机对单表的并行加载，最大化提升加载性能；
- 4）支持从通用数据服务器拉取数据，支持 ftp/http/hdfs/Kafka/sftp/https/AMAZON S3 等多种文件传输协议；
- 5）支持普通文本（csv、txt 等）、json、gzip 压缩、snappy 压缩、lzo 压缩等多种格式数据文件；
- 6）支持普通文本、定长文本、宽松模式的加载；
- 7）支持错误数据溯源功能，可以准确定位错误数据在源文件中的位置；
- 8）加载性能可以随着集群规模的扩展而持续提升。

---
### Q13 — GBase 8s 最小 IO 单元

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** GBase 8s数据库最小的IO单元是？

**选项:**
1. row
2. page ✅
3. extent
4. chunk

**我的答案:** 选项1 ❌

**正确答案:** 选项2

**解析:**
- GBase 8s数据库最小的IO单元是page

---
### Q14 — GBase USING HASH GLOBAL 建表类型

**来源:** 新版题库 Excel（2026-09-25 同步）

**题目:** 以下SQL语句创建的是哪种类型的表？
```
CREATE TABLE t1(a int , b varchar(10), KEY a_key (a) USING HASH GLOBAL);
```

**选项:**
1. 索引表
2. 随机分布表 ✅
3. hash分布表
4. 临时表

**我的答案:** 选项3 ❌

**正确答案:** 选项2

**解析:**
- 默认创建的是随机分布表

---
### Q15 — GBase SQL JOIN 多表连接

**来源:** 每日一练 App

**题目:** 下列哪个 SQL 不正确？

**TAB_CI 表:**

| CI | CINAME |
|:---:|:---:|
| 12345 | 小区1 |
| 23456 | 小区2 |
| 45678 | 小区4 |
| 56789 | 小区5 |

**TAB_PARAMETER 表:**

| CI | P1 | P2 |
|:---:|:---:|:---:|
| 12345 | 1.10 | 2.20 |
| 23456 | 2.10 | 3.20 |
| 4567 | 3.10 | 4.20 |

**期望输出**（CI 匹配 12345 和 23456，即内连接 2 行）:

| CI | CINAME | P1 | P2 |
|:---:|:---:|:---:|:---:|
| 12345 | 小区1 | 1.10 | 2.20 |
| 23456 | 小区2 | 2.10 | 3.20 |

**选项:**
1. 
  ```  
  SELECT A.CI,A.CINAME,B.P1,B.P2 FROM TAB_CI A JOIN TAB_PARAMETER B ON A.CI=B.CI;  
  ```
2. 
  ```  
  SELECT A.CI,A.CINAME,B.P1,B.P2 FROM TAB_CI A LEFT JOIN TAB_PARAMETER B ON A.CI=B.CI;  
  ``` ✅
3. 
  ```  
  SELECT A.CI,A.CINAME,B.P1,B.P2 FROM TAB_CI A, TAB_PARAMETER B WHERE A.CI=B.CI;  
  ```
4. 
  ```  
  SELECT * FROM TAB_CI JOIN TAB_PARAMETER USING(CI);  
  ```

**我的答案:** 选项3 ❌
**正确答案:** 选项2（```）

**解析:**
- 期望结果只有 2 行（CI 匹配 12345 和 23456），即**内连接**结果
- 逐一分析：
  - 选项1：`JOIN ... ON` → 标准内连接，结果 2 行 ✅
  - 选项2：`LEFT JOIN` → 左外连接，保留 TAB_CI 全部 4 行，未匹配的（45678 小区4、56789 小区5）P1/P2 为 NULL → 结果 **4 行**，与期望不符 ❌ **这是不正确的那条**
  - 选项3：逗号分隔 + WHERE → 实际上是隐式内连接，等价于选项1，结果 2 行 ✅。用户选了这项是误以为逗号写法不等于 JOIN——其实标准 SQL 中 `FROM A, B WHERE A.id = B.id` 和 `FROM A JOIN B ON A.id = B.id` 等价
  - 选项4：`JOIN ... USING(CI)` → 也是内连接，`USING` 是 ON 的简写形式（当两表列名相同时），结果 2 行 ✅
- 记忆点：逗号连接 + WHERE = 内连接（不是错误的）；LEFT JOIN 会多出未匹配行，与内连接结果不同

---

---

## 📊 第五周错题汇总

| 日期 | 题数 | 答对 | 答错 |
|:----:|:----:|:----:|:----:|
| 08-10 | 5 | 3 | 2 |
| 08-12 | 10 | 2 | 8 |
| **合计** | **15** | **5** | **10** |

- 📌 Excel 题库同步（2026-09-25，选项顺序已同步）：
  解析：选项`SELECT A.CI,A.CINAME,B.P1,B.P2 FROM TAB_CI A LEFT JOIN TAB_PARAMETER B ON A.CI=B.CI;`写法错误。正确写法是`SELECT A.CI,A.CINAME,B.P1,B.P2 FROM TAB_CI A JOIN TAB_PARAMETER B ON A.CI=B.CI;`，其他三项都对
