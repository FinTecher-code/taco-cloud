# 第三周 - 刷题记录

---

## 2026-07-21

---

### Kafka — retries 与 retries.backoff.ms 配置

**题目：** 关于 Kafka 生产者的 `retries` 和 `retries.backoff.ms` 参数，下列描述**不正确**的是？

**选项：**
1. `retries` 是生产消息的重试次数
2. `retries` 设置重试的时间间隔，单位是毫秒 ❌（**不正确的说法，正确答案**）
3. `retries.backoff.ms` 设置重试的时间间隔，单位是毫秒
4. `retries` 设置为 0 表示不重试

**我的答案：** 选项3 ❌
**正确答案：** 选项2 ✅

**解析：**
这道题的陷阱是把 `retries` 和 `retries.backoff.ms` 的职责搞混了：

| 参数 | 作用 | 默认值 |
|------|------|:------:|
| `retries` | 重试**次数**，不是时间间隔 | `Integer.MAX_VALUE`（新版） |
| `retries.backoff.ms` | 重试之间的**时间间隔**（毫秒） | `100` |

- **选项2** ❌ 错误地把 `retries` 说成是重试间隔，实际上重试间隔由 `retries.backoff.ms` 控制
- **选项3** ✅ 本身描述正确，`retries.backoff.ms` 确实是重试间隔，单位毫秒
- 你选了选项3，可能是因为题目看反了——题目问的是"不正确的"，而选项3的描述本身是对的，应该是选选项2才对

**关键记忆：**
- `retries` → 次数（多少次）
- `retries.backoff.ms` → 间隔（多久一次）
- 记一句口诀：**次数是 retries，间隔找 backoff**

---

### Kafka — 增加主题分区

**题目：** 某团队正在开发一个实时日志处理系统，使用 Kafka 作为消息传递的中间件，以下哪个操作可以增加一个主题的分片数量？

**选项：**
1. 删除一个分区
2. 启用压缩 ❌（我的答案）
3. 增加副本数
4. 修改分片配置 ✅（**正确答案**）

**我的答案：** 启用压缩 ❌
**正确答案：** 修改分片配置 ✅

**解析：**
- 要增加 Kafka 主题的分区（分片）数量，需要使用 **`kafka-topics.sh --alter --partitions <N>`** 命令，本质上就是修改主题的分区配置
- **启用压缩**（`compression.type`）只是控制消息压缩方式，跟分区数量完全无关
- **增加副本数** 是调整数据冗余，不影响分区数量
- **删除一个分区** ❌ Kafka 不支持减少分区（只能增加，不能减少）

> ⚠️ 注意：增加分区数后，原有键到分区的映射关系会改变，可能影响消息顺序保证（同一个 key 的消息可能被路由到不同分区）

---

### Kafka — 数据管道概念

**题目：** Kafka 中的数据管道指的是什么？

**选项：**
1. ✅ 不同应用程序之间传输和处理数据的通信机制（正确答案）
2. ❌ 消息的持久化存储机制
3. ❌ 集中控制所有生产者和消费者的中心化组件
4. ❌ 将消息从一个节点分发到其他节点的机制

**我的答案：** 选项1 ✅
**正确答案：** 选项1

**解析：**
Kafka 数据管道（Data Pipeline）的核心定义是**不同应用之间传输和处理数据的通信机制**：

- Kafka 作为数据管道，核心价值在于**解耦**生产者和消费者
- 支持**多订阅者**模式，一份数据可以被多个消费者独立消费
- 支持**回溯重放**，消费者可以从任意 offset 重新消费
- 不是单纯的存储系统，也不是点对点的消息队列

---

### Kafka — 消费者组与分区消费

**题目：** Kafka 中，一个消费者组可以同时消费一个主题的多个分区吗？

**选项：**
1. ✅ 可以，消费者组可以同时消费一个主题的多个分区（**正确答案**）
2. ❌ 不可以，一个消费者组只能消费一个主题的一个分区（我的答案）
3. ❌ 可以，但是需要使用特定的配置参数
4. ❌ 不可以，一个消费者组只能消费一个主题的所有分区

**我的答案：** 选项2 ❌
**正确答案：** 选项1 ✅

**解析：**
Kafka 中消费者组的分区分配机制：

- **消费者组作为一个整体，可以消费一个主题的所有分区**
- 组内每个分区**只能被一个消费者消费**（同一分区不会分配给组内两个消费者）
- 但一个消费者**可以消费多个分区**
- 通过增加组内消费者数量，可以提升并行消费能力

**举例：** 一个主题有 6 个分区，一个消费者组有 3 个消费者 → 每个消费者消费 2 个分区，实现了并行消费。

**关键规则：**
- 分区数 ≥ 消费者数 → 每个消费者平均分配分区
- 分区数 < 消费者数 → 部分消费者空闲（一个分区只能给一个消费者）

**你选选项2的原因：** 可能把"一个分区只能被组内一个消费者消费"误解成"一个消费者组只能消费一个分区"了。前者是对的，后者是错的——组内不同消费者可以各自消费不同的分区。

---

### Kafka — 过期数据清理策略

**题目：** 过期数据清理中启用删除策略的配置是？

**选项：**
1. `log.cleanup.policy=none`
2. `log.cleanup.policy=all`
3. ✅ `log.cleanup.policy=delete`（**正确答案**）
4. `log.cleanup.policy=compact`

**我的答案：** 选项3 ✅
**正确答案：** 选项3

**解析：**
Kafka 的日志清理策略（`log.cleanup.policy`）只有两种：

| 值 | 作用 |
|:--:|:----:|
| `delete` | **启用删除策略**，根据时间或大小阈值删除过期数据（默认） |
| `compact` | **启用压缩策略**，保留每个 key 的最新一条消息 |

- 默认值：`delete`
- 可以同时配置两种：`log.cleanup.policy=delete,compact`
- `none` 和 `all` 都是干扰项，不存在

---

---

## 2026-07-22

---

### Kafka — RoundRobin 策略前提条件

**题目:** Kafka 使用 RoundRobin 策略有两个前提条件，其中**不包括**？

**选项:**
1. 同一个 Consumer Group 里面的所有消费者的 num.streams 必须相等
2. 每个消费者订阅的主题必须相同
3. ✅ 每个消费者订阅的 Broker 必须相同 (**正确答案**)
4. 其他都不对

**我的答案:** 选项1 ❌
**正确答案:** 选项3 ✅

**解析:**
RoundRobin 分配策略的两个真正前提条件是：

| 前提条件 | 说明 |
|:--------|:----|
| **num.streams 相等** | 同一个 Consumer Group 内所有消费者的 num.streams 必须一致 |
| **订阅主题相同** | 每个消费者订阅的主题列表必须完全一致 |

- **选项1** 是 RoundRobin 正确前提条件之一，所以它**不是**题目问的“不包括”项
- **选项3**（每个消费者订阅的 Broker 必须相同）才是真正**不包含**的——Broker 地址跟分区分配策略无关

**关键记忆:**
> RoundRobin 两大前提：**num.streams 相等** + **订阅主题相同**
> Broker 地址不影响分配策略

---

### Kafka — Producer 优化写入速度

**题目:** 关于 Kafka producer 优化写入速度的方法，下列哪项是**错误**的？

**选项:**
1. ✅ 增加 ISR 数量 (**正确答案**)
2. 增加 partition 数
3. 提高 batch.size
4. 增加线程

**我的答案:** 增加 ISR 数量 ✅
**正确答案:** 选项1 ✅

**解析:**
Kafka producer 优化写入速度的有效方法包括：

| 有效方法 | 说明 |
|:--------|:----|
| **增加线程** | 多线程并发发送，提高吞吐量 |
| **提高 batch.size** | 增大批量大小，减少网络开销 |
| **增加更多 producer 实例** | 多实例并行写入 |

- **增加 ISR 数量** ❌ ISR 是副本同步相关参数，影响的是数据**可靠性/一致性**，而不是写入速度
- 调大 ISR 反而可能**降低**写入速度（需要等待更多副本确认）

**关键记忆:**
> 写入速度优化：**线程 + batch.size + 多实例**
> ISR 管的是**可靠性**，不是速度

---

---

### Kafka — 消费者未提交 offset

**题目:** Kafka 中如果消费者消费后没有提交 offset，会怎样？

**选项:**
1. ❌ 程序崩溃（我的答案）
2. 强行 kill
3. ✅ 重复消费（正确答案）
4. 自动消费

**我的答案:** 程序崩溃 ❌
**正确答案:** 重复消费 ✅

**解析:**
- Kafka 消费者消费消息后，如果没有提交 offset，下次启动或 rebalance 时，会从上次提交的 offset 位置重新消费
- 这导致**重复消费** (Duplicate Consumption)，而不是程序崩溃
- Kafka 消费者有自动提交机制 (enable.auto.commit=true)，但即使关闭自动提交，也只是不提交 offset，不会导致程序崩溃

**关键记忆:**
> 不提交 offset → 重复消费，不是崩溃
> Kafka 保证 at least once 语义，重复消费是常见现象

---

---

### Kafka — 分区键映射

**题目:** 关于 Kafka 分区位置描述错误的是？

**选项:**
1. ✅ 主题内添加了新的分区，散列算法是恒定的，键与分区之间的映射一定不会发生改变（正确答案）
2. 如果键值不为 null 并使用了默认的分区器，消息就会写在特定的分区上，如果该分区不可用就会发生错误
3. ❌ 只有在不改变主题分区数量的情况下，键与分区之间的映射就会保持不变（我的答案）
4. 主题内添加了新的分区，键与分区之间的映射可能会发生改变

**我的答案:** 选项3 ❌
**正确答案:** 选项1 ✅

**解析:**
- Kafka 默认分区策略：对 key 求散列后对分区数取模：data(key).hashCode() % numPartitions
- 当分区数改变时，取模结果会变，所以同一个 key 可能被路由到不同的分区
- **选项1** 说分区数改变后映射不变——这是错的，或许是正确答案（题目问的是“错误的是”）
- **选项3** 其实是**正确的**描述：分区数不变，映射确实不变
- 你选了选项3，说明你认为它是错的，但它其实是对的，这道题的线企在于把看似正确的选项1当作了事实

**关键记忆:**
> 分区数变→ 映射变
> 分区数不变→ 映射不变
> 散列算法恒定但取模结果会变

---

---

### Kafka — 心跳参数

**题目:** 下列关于 Kafka 消费者心跳描述错误的是？

**选项:**
1. ✅ 一般情况下 heartbeat.interval.ms 的时间会设置得比 session.timeout.ms 要大，session.timeout.ms 的默认值是 30s（正确答案）
2. ❌ session.timeout.ms 指定消费者被认定死亡之前可以与服务器断开连接的时间（我的答案）
3. heartbeat.interval.ms 指定了向协调器发送心跳的频率
4. 把 session.timeout.ms 值设置的大一点可以减少意外的再均衡

**我的答案:** 选项2 ❌
**正确答案:** 选项1 ✅

**解析:**
- 这道题的核心要记住：**heartbeat.interval.ms** **** **session.timeout.ms**（心跳间隔小于会话超时）
- 典型配置：heartbeat.interval.ms=3s，session.timeout.ms=45s
- **选项1** 说 heartbeat 比 session timeout 大——这是错的，所以它是正确答案（题目问“错误的是”）
- **选项2** 其实是对 session.timeout.ms 的**正确描述**
- **选项4** 也是对的：增大 session.timeout.ms 可以给消费者更多时间发送心跳，减少不必要的 rebalance

**关键记忆:**
> **heartbeat.interval.ms**（3s）< **session.timeout.ms**（45s）
> 心跳间隔比会话超时小，不是大。

---

---

### Git — vs SVN 区别

**题目:** 下列关于 git 和 svn 区别中说法错误的是？

**选项:**
1. git是分布式的，svn是集中式的
2. git把内容按元数据的方式存储，svn按文件存储，git的内容完整性要优于svn ❌（我的答案）
3. git有一个全局的版本号，而svn没有 ✅（正确答案）
4. git不需要联网就可以使用（本地库），svn需要联网

**我的答案:** 选项2 ❌
**正确答案:** 选项3 ✅

**解析:**
Git 和 SVN 的核心区别：

| 特性 | Git | SVN |
|:----|:---|:---|:--|
| 架构 | 分布式，本地完整仓库 | 集中式，需连接中央服务器 |
| 版本号 | **SHA-1 哈希**，无全局顺序号 | **顺序数字**（r1, r2, r3...） |
| 离线 | 完整本地仓库，可离线工作 | 必须联网访问中央仓库 |
| 内容存储 | 内容可寻址对象存储（blob/tree/commit） | 文件系统+增量存储 |
| 完整性 | SHA-1 哈希校验，完整性高 | 文件级校验 |

- **选项2** 的描述不算错（Git 的内容完整性确实优于 SVN），但这不是题目设问的重点
- 本题的**核心考点**是版本号：SVN 有全局顺序版本号（r1, r2, r3...），Git 用 SHA-1 哈希，**没有**全局版本号
- **选项3** 说反了，所以它是错误的说法，即本题正确答案

### Git — 生成 SSH 公钥步骤

**题目:** Git 生成 ssh 公钥的正确步骤是？

**选项:**
1. 下载git → 安装 → cd ~ → mkdir .ssh → cd .ssh → 配置name/email → 生成key → 回车 ❌（我的答案）
2. 下载git → 安装 → 配置name/email → mkdir .ssh → cd .ssh → cd ~ → 生成key → 回车
3. 下载git → 安装 → 配置name/email → cd ~ → mkdir .ssh → cd .ssh → 生成key → 回车 ✅（正确答案）
4. 下载git → 安装 → mkdir ~/.ssh → cd ~ → cd .ssh → 配置name/email → 生成key → 回车

**我的答案:** 选项1 ❌
**正确答案:** 选项3 ✅

**解析:**
生成 SSH 公钥的正确流程：
1. 下载并安装 Git
2. 配置全局 name 和 email（`git config --global user.name` / `user.email`）
3. 打开 Git Bash，`cd ~` 进入用户主目录
4. `mkdir .ssh` 创建 .ssh 文件夹
5. `cd .ssh` 进入文件夹
6. `ssh-keygen -t rsa -b 4096 -C "email"` 生成密钥
7. 回车确认默认路径，可选设置密码

- **选项1** 把 `cd ~` → `mkdir .ssh` → `cd .ssh` 放在了配置 name/email **之前**，顺序不正确
- **选项2** 在创建 `.ssh` 文件夹和进入目录后才 `cd ~` 回到主目录，逻辑错乱
- **选项3 ✅** 是最合理的顺序：先配置 → `cd ~` 进主目录 → `mkdir .ssh` → `cd .ssh` → 生成密钥
- **选项4** 用绝对路径创建 `.ssh` 再 `cd ~` 也能工作，但不如选项3自然

**关键记忆:**
> SSH 生成流程：**安装 → 配置 → cd ~ → mkdir .ssh → cd .ssh → ssh-keygen → 回车**

---

### Git — rebase --onto 用法

**题目:** 取出 client 分支，找出处于 client 分支和 server 分支的共同祖先之后的修改，然后把它们在 master 分支上重放一遍。以下哪个命令能达到上述效果？

**选项:**
1. `git rebase --onto master server client` ✅（正确答案）
2. `git rebase --onto server client master`
3. `git rebase --onto master client server` ❌（我的答案）
4. `git rebase --onto client server master`

**我的答案:** 选项3 ❌
**正确答案:** 选项1 ✅

**解析:**
`git rebase --onto` 语法格式：

> `git rebase --onto <新基线> <上游> <分支>`

含义：检出 `<分支>`，找出其与 `<上游>` 的共同祖先之后的提交，将这些提交**重放（replay）**到 `<新基线>` 上。

| 选项 | 解析 |
|:----:|:----|
| 1 ✅ | `--onto master`（目标基线）→ `server`（上游比较）→ `client`（被重放的分支）—— 取出 **client** 中比 server 多出的提交，放到 **master** 上 ✅ |
| 2 | `--onto server` → `client` → `master` —— 把 **master** 中比 client 多出的提交放到 **server** 上 |
| 3 ❌ | `--onto master` → `client` → `server` —— 把 **server** 中比 client 多出的提交放到 **master** 上 ❌（不是 client） |
| 4 | `--onto client` → `server` → `master` —— 把 **master** 中比 server 多出的提交放到 **client** 上 |

**关键记忆:**
> `rebase --onto A B C` = 把 C 分支中超出 B 的提交重放到 A 上
> 三个参数顺序：**目标 → 上游 → 分支**

---

### Git — merge vs rebase 优势

**题目:** 相比于 `git rebase`，`git merge` 的一个优势是什么？

**选项:**
1. git merge 保留了分支的完整历史和时间顺序，而 git rebase 则没有 ✅（正确答案）
2. git merge 不会在项目历史中添加新的提交，但 git rebase 会
3. git merge 过程中的数据冲突会自动解决，但 git rebase 不会 ❌（我的答案）
4. git merge 上的提交被缓存，变成比 git rebase 更快的操作

**我的答案:** 选项3 ❌
**正确答案:** 选项1 ✅

**解析:**

| 选项 | 分析 |
|:----:|:----|
| 1 ✅ | merge 会保留分支的**完整历史**和**时间顺序**，产生的 merge commit 清晰记录了合并来源；rebase 会**重写历史**，让提交历史变成线性，但丢失了实际的时间线信息 ✅ |
| 2 ❌ | merge **会**创建 merge commit（新提交），rebase 不会产生 merge commit（但会创建新的提交副本） |
| 3 ❌ | **两者都不会自动解决冲突**，冲突都需要**手动解决**。这是常见误解！ |
| 4 ❌ | 不存在“缓存提速”机制，性能差异不显著 |

**核心区别：**
- **merge**：保留真实历史，适合公共分支协作
- **rebase**：线性历史，适合个人特性分支整合前清理提交记录

**关键记忆:**
> merge 管**历史真实性**，rebase 管**历史整洁性**
> 冲突都得自己解，谁也帮不了谁 🤯

---

### Git — 纯本地操作

**题目:** 下列哪个操作是纯本地的，不涉及远程仓库的交互或配置？

**选项:**
1. `git clone git@xxxx/xxxx.git`
2. `git remote add origin git@xxxx/xxxx.git`
3. `git merge` ✅（我的答案）
4. `git pull --rebase`

**我的答案:** 选项3 ✅
**正确答案:** 选项3 ✅

**解析:**

| 选项 | 是否纯本地 | 原因 |
|:----:|:----------:|:----|
| 1 ❌ | ❌ | `git clone` 需要从远端仓库拉取代码 |
| 2 ❌ | ❌ | `git remote add` 虽不联网，但涉及**远程仓库配置**（题目要求也不涉及配置） |
| 3 ✅ | ✅ | `git merge` 完全在本地仓库操作，合并两个本地分支，不涉及任何远端 |
| 4 ❌ | ❌ | `git pull = git fetch + git merge`，`fetch` 需要连接远端 |

**关键记忆:**
> 纯本地操作：**merge、checkout、branch（本地）、commit、diff、log、status** 等
> 涉及远端：**clone、push、pull、fetch、remote**

---

## 📊 第三周错题汇总

| 日期 | 题数 | 答对 | 答错 |
|:----:|:----:|:----:|:----:|
| 07-21 | 5 | 2 | 3 |
| 07-22 | 5 | 1 | 4 |
| 07-26 | 5 | 1 | 4 |
| **合计** | **15** | **4** | **11** |
